CREATE DEFINER=`root`@`localhost` PROCEDURE `webhook_trackupdate_17track`( in p_bill_ref VARCHAR(30), in p_bill_carrier VARCHAR(30), in p_billcode VARCHAR(30), in p_status VARCHAR(100), in p_note VARCHAR(200), in p_location VARCHAR(100), in p_city VARCHAR(30),in p_country varchar(30), in p_date_journey DATETIME)
begin

	insert into order_tracking_journey ( bill_code_ref, carrier, bill_code, status, note, location, city, country, date_journey, provider) values
	(
		p_bill_ref,
		p_bill_carrier,
		p_billcode,
		p_status,
		p_note,
		p_location,
		p_city,
		p_country,
		p_date_journey,
		p_bill_carrier

	);
end
